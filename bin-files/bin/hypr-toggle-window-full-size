#!/usr/bin/env bash

STATE_FILE="/tmp/hypr_toggle_scrolling_focus"
COMMAND=(hyprctl dispatch layoutmsg colresize)

if [[ -f "$STATE_FILE" ]]; then
  rm "$STATE_FILE"
  "${COMMAND[@]}" 0.97
else
  touch "$STATE_FILE"
  "${COMMAND[@]}" 1.0
fi

