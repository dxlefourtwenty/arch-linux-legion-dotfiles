#!/usr/bin/env bash

set -euo pipefail

IMAGE_EDITOR="satty"
FLAGS=(--floating-hack --resize smart)

# Exit if nothing was passed
[ -z "$1" ] && exit 1

# Launch detached so it does not occupy the current terminal.
nohup "$IMAGE_EDITOR" "${FLAGS[@]}" -f "$1" >/dev/null 2>&1 &
