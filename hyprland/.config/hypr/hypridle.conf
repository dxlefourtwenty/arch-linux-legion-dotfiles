general {
    lock_cmd = pidof swaylock || /home/dxle/bin/launch-lock-screen       # avoid starting multiple swaylock instances.
    before_sleep_cmd = /home/dxle/bin/launch-lock-screen    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on  # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 10                                 # 10min
    on-timeout = loginctl lock-session            # lock screen when timeout has passed
}

listener {
    timeout = 15
    on-timeout = hyprctl dispatch exec "w" kitty --class blackout sh -c 'printf "\033[48;2;0;0;0m"; clear; sleep infinity'
    on-resume = pkill -f "kitty --class blackout"
}

